Antes de correr hay que setear el path de SDK para android y agregar esta linea

~~~xml
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
~~~

a /android/app/src/main/AndroidManifest.xml
